from __future__ import annotations

from decimal import Decimal

# Smart listing assistant (rule-based, no LLM).
MAX_DESCRIPTION_LENGTH = 600
PROMOTION_PRICE_KGS = Decimal("50")
PROMOTION_CURRENCY = "KGS"
