[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: optional real-DB regression tests
filterwarnings =
    ignore::DeprecationWarning
